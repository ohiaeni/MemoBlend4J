/**
 * @export
 * @interface CalendarEvent
 * @property {number} id
 * @property {string} title
 * @property {Date} date
 * @property {string} color
 */
export interface CalendarEvent {
  id: number;
  title: string;
  date: Date;
  color: string;
}
